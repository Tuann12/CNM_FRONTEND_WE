import classNames from 'classnames/bind';
import styles from './InputChat.module.scss';

const cx = classNames.bind(styles);

function InputChat() {
    return <div className={cx('wrapper')}>dsadsad</div>;
}

export default InputChat;
